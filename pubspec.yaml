name: tensor_graph
description: A starting point for Dart libraries or applications.
version: 0.0.1
author: Roberto Tassi <roberto.tassi@fromlabs.org>
homepage: https://github.com/fromlabs/tensor_graph

environment:
  sdk: '>=1.20.1 <2.0.0'

dependencies:
  tensor_math: ^0.0.1
  meta: ^1.0.4
  collection: ^1.13.0

dev_dependencies:
  test: ^0.12.0

dependency_overrides:
  tensor_math:
    path: ../tensor_math